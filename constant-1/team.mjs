const API_BASE_URL='/api'

const CONSTANT ={
    team : {
        add : `${API_BASE_URL}/team/add`,
        read : `${API_BASE_URL}/team/read`,
        find : `${API_BASE_URL}/team/find/:id`,
        // updateRole : `${API_BASE_URL}/team/updateRole/:id`,
    }
}
export default CONSTANT